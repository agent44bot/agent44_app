<% content_for :title, "On-demand team report" %>

<div class="max-w-3xl mx-auto px-4 py-6">
  <div class="flex items-center justify-between gap-3 mb-4">
    <div>
      <h1 class="text-lg font-semibold text-gray-100">Team report</h1>
      <p class="text-xs text-gray-500">Fresh copy generated just now<%= @report_snapshot_date ? " (data from #{@report_snapshot_date.strftime('%b %-d, %Y')})" : "" %>.</p>
    </div>
    <%= link_to "Back", nyk_analyst_path, class: "text-sm text-orange-400 hover:text-orange-300" %>
  </div>

  <%# How it works: a short walkthrough Lora can read before using it. %>
  <details class="bg-gray-900 border border-gray-800 rounded-xl p-4 mb-4" open>
    <summary class="text-sm font-medium text-gray-200 cursor-pointer">How it works (quick demo)</summary>
    <ol class="list-decimal ml-5 mt-3 space-y-1 text-sm text-gray-400">
      <li>Open the Analyst dashboard and tap <span class="text-gray-200 font-medium">Generate report</span> any time you need a fresh copy, like before a board meeting.</li>
      <li>This page shows the full report. Tap <span class="text-gray-200 font-medium">Print or save as PDF</span> to take it into a meeting.</li>
      <li>To send it on, type an email address (a board member, an outside developer, or yourself) and tap <span class="text-gray-200 font-medium">Email report</span>.</li>
    </ol>
  </details>

  <%# Toolbar: print + email-to-anyone. %>
  <div class="bg-gray-900 border border-gray-800 rounded-xl p-4 mb-4">
    <button type="button" onclick="window.print()"
            class="inline-flex items-center px-3 py-2 rounded-lg bg-gray-800 text-gray-100 text-sm font-medium hover:bg-gray-700 mb-3">
      Print or save as PDF
    </button>

    <%= form_with url: nyk_send_report_path, method: :post, class: "flex flex-col sm:flex-row gap-2" do |f| %>
      <%# 16px (text-base) so iOS doesn't zoom the input on focus. %>
      <%= email_field_tag :email, nil, required: true, placeholder: "name@example.com",
            class: "flex-1 text-base px-3 py-2 rounded-lg bg-gray-950 border border-gray-700 text-gray-100 placeholder-gray-600" %>
      <%= f.submit "Email report", class: "px-4 py-2 rounded-lg bg-orange-500 text-white text-sm font-medium hover:bg-orange-400 cursor-pointer" %>
    <% end %>
    <p class="text-xs text-gray-600 mt-2">The report is sent from your NY Kitchen team. We do not store the recipient beyond a usage record.</p>
  </div>

  <%# The report itself, isolated in an iframe so its email styles don't bleed
      into the app shell (and vice versa). %>
  <div class="bg-white rounded-xl overflow-hidden border border-gray-800">
    <iframe title="Team report preview" style="width:100%;height:1400px;border:0;background:#ffffff" srcdoc="<%= h(@report_html) %>"></iframe>
  </div>
</div>
